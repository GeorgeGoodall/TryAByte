=== ToDo ===
============
	Restaurant UI
		- refresh order view if status updated
		- remove show address from restaurant view
		- add notification for restaurant order made
		- menu formatting errors on food delete if name too long
		- menu commit clears instantly after commit, consider making it clear at another time
	
	Rider UI
		- offered order doesnt get added to your orders after you offer
		- submitting key doesn't remove check key input and address request
		- refresh order view if status updated
		- rider restaurants view showing incorrect number of orders for each restaurant
		- rider deposit paid alert needs changing to make sense
		- rider view, print food items when delivery offered
	
	Customer UI
		- refresh order view if status updated

	General UI
		- make better use of await to speed up processing massivly
		- have relivent orders appear at top
		- remove buttons so only ones currently used are shown
		- remove complete orders somewhere else

	General
		- have all libraries imported from online imported from local
		- order id'ing method not consistant between your orders and restaurant available orders on rider view (change to id by contract address)
		- add button to remake key, in case of key loss
		- improve program file layout, split into server and client
		- remove unused node modules
		- fix git repo
		- resolve metamask sometimes not passing address through
		- move redundant js code into some form of library and import to make neater
		- manage local storage to remove unneeded keys
	
	Testing ToDo
		- test removeing factory references
		- test menu delete, seems buggy when deleting large number of items

	Would be nice to do
		- add alerts to explain setting up of metamask
		- store all user information within mongodb
		- impove delivery address form
		- after creating an accout, navigate to some form of waiting information page that gives tips etc
		- Bug: failing adding to the menu doen't clear the staging, only clears the staging display
		- resolve slow loading time on order view (preload stuff in background)
		- have metamask set a default gas price
		- add contract value under order window
		- resolve issue where two contract calls happens if two pages open


- smart contract improvments
	- make account contracts inherit from an account contract
	- add short by to revert statment for not enough eth sent
	- optimise backend for gas price
	- store hashed user data including address to validate user information